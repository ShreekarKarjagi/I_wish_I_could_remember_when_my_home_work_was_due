# secrets and per-user state — never commit these
# (credentials.json is intentionally NOT ignored: it's a public desktop OAuth client id, see README)
.env
token.json*
synced.json
sync.log

# python / IDE
__pycache__/
*.pyc
.venv/
venv/
.idea/
.vscode/
